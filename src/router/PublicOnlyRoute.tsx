/**
 * @file PublicOnlyRoute.tsx
 * @description Stub public-only route wrapper that simply renders nested routes in Phase 4.
 * @module src/router/PublicOnlyRoute
 */
import { Outlet } from 'react-router-dom';

export const PublicOnlyRoute = () => <Outlet />;
